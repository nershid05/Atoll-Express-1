## Packages
framer-motion | Complex animations and page transitions
date-fns | Date and time formatting
react-hook-form | Form state management
@hookform/resolvers | Zod validation integration for forms
clsx | Conditional class names
tailwind-merge | Merging tailwind classes safely

## Notes
- Auth is handled via Replit Auth (useAuth hook)
- Booking flow: Select Trip -> Enter Details -> View Ticket
- Admin pages are protected by isAuthenticated check
- Images use Unsplash source URLs
